overwrite: true
schema: "http://localhost:5000/graphql"
documents: "src/**/*.{ts,tsx}"
generates:
  schemas/generated/apollo-components.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
